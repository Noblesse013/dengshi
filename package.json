{
  "name": "dengshi-root",
  "private": true,
  "scripts": {
    "dev": "cd dengshi && npm run dev",
    "build": "cd dengshi && npm run build",
    "start": "cd dengshi && npm run start",
    "install:app": "cd dengshi && npm install"
  }
}
